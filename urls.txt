http://localhost:5000/.well-known/openid-configuration

Post to http://localhost:5000/connect/token
    client_id=oauth_client
    client_secret=shhh
    scope=api1.read
    grant_type=client_credentials

Use https://jwt.io to decode the token
